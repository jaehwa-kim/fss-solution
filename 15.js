// primitive type and object type 차이점 


// primitive type = number, string, boolean, null, undefined
// object type = array, object, function
// primitive object type 은 서러 저장 방식이 다르다.

// 변수 이름에 의미를

// let 변수,

// const 상수,

const obj = {
    name: '재화',
    age: 39,
    job: 'multifuntion'
}

console.log(obj.name);

obj.job = 'pretty';

console.log(obj.job)